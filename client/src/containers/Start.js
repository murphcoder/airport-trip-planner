import React from 'react';
import NavBar from '../components/navBar';
import { URL } from '../App'

const Start = () => {

    return (
        <div>
            <NavBar url={URL} />
        </div>
    )

};

export default Start;